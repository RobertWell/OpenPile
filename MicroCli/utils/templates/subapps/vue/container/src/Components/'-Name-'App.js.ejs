import {mount} from '{|{Name|lower}|}/{|{Name|capitalize }|}App';
import React, {useRef, useEffect} from "react";


export default () => {
    const ref = useRef(null);

    useEffect(() => {
        mount(ref.current);
    }, []);

    return <div ref={ref}/>;
}
